---
- name: Prepare container — install Python
  hosts: all
  gather_facts: false
  tasks:
    - name: Install Python 3
      raw: apt-get update && apt-get install -y python3 python3-pip
      changed_when: false
